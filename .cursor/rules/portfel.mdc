---
description: Portfel — личный учёт портфеля Т‑Инвестиций. Обязательный контекст.
alwaysApply: true
---

Перед правками читай `AGENTS.md` в корне: карта кода, метрики, ловушки, как гонять Docker/тесты.

Кратко: ответы на русском; UI на русском; TZ Europe/Moscow; не коммитить без просьбы; после правок `docker compose up --build` (исходники в контейнер не монтируются). Стоимость = позиции×цена+кэш; вложено = чистые INPUT/OUTPUT, не себестоимость. Фоновые job'ы только через `from app import db as db_module` и `db_module.SessionLocal`.
